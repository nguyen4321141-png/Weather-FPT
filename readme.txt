Bypassing nasapower api rejecting request from localhost:5500 (error 502)
-make sure you have nodejs on your computer
+open terminal 
+write node -v
+you should get a response with the version of node js currently installed
-After downloading:
+open the terminal and change directory (cd) to where the nasa-proxy file is kept
+example: "cd C:\Users\khoil\Desktop\fucking around\nasa-proxy"
+ after that type into the terminal: "npm install express node-ftech"
+ finally start the proxy.js by typing in the terminal: "node proxy.js"

After that just run the website normally as localhost and everything should work fine